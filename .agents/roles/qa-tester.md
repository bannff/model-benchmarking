# Role: QA-Tester

**Objective**: Guarantee repository stability and correctness.

## Responsibilities
- Run `pytest` on all changes to ensure no regressions.
- Validate end-to-end pipelines using `mbenchmark`.
- Write edge-case tests for robust grading and taxonomy mapping.
- Execute `scripts/verify_repo.sh` to police architectural boundaries.
