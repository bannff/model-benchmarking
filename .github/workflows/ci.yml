name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Default to least privilege; jobs elevate as needed.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # required for GHCR push on main
    strategy:
      fail-fast: false
      matrix:
        python-version: [ '3.10', '3.11', '3.12' ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip setuptools wheel

      - name: Install dependencies (if present)
        run: |
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi

      - name: Lint with ruff
        run: |
          ruff check src tests || (echo "Lint failed" && exit 1)

      - name: Type check with mypy
        run: |
          mypy src || (echo "Type check failed" && exit 1)

      - name: Install package
        run: |
          pip install .

      - name: Import check
        run: python3 -c "import sys; sys.path.insert(0, 'src'); import model_benchmarking; print('import ok', model_benchmarking)"

      - name: Run basic CLI help check
        run: mbenchmark --help

      - name: Run tests
        run: pytest -q

      - name: Build Docker image (local tag)
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          push: false
          tags: model-benchmarking:ci

      - name: Publish to GHCR (uses GITHUB_TOKEN, optional GHCR_PAT fallback)
        # Only publish :latest from main on push events, never from PRs or feature branches.
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && matrix.python-version == '3.11'
        env:
          CR_PAT: ${{ secrets.GHCR_PAT || github.token }}
        run: |
          # Use the provided GHCR_PAT if set, otherwise GITHUB_TOKEN (works for in-repo publishing)
          echo "$CR_PAT" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker tag model-benchmarking:ci ghcr.io/${{ github.repository_owner }}/model-benchmarking:latest
          docker push ghcr.io/${{ github.repository_owner }}/model-benchmarking:latest

  smoke-pipeline:
    name: Smoke pipeline (mock provider)
    runs-on: ubuntu-latest
    needs: test-and-build
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
          pip install .

      - name: Run pipeline with mock provider (skip cs-eval)
        run: |
          mbenchmark pipeline \
            --provider mock \
            --model mock \
            --max_questions 1 \
            --output_dir results \
            --skip-cs-eval \
            --verbose
